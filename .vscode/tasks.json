{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project",
            "type": "shell",
            "command": "cmake -S . -B build/ -DCMAKE_BUILD_TYPE=Debug -DBUILD_SHARED_LIBS=OFF -DEMBER_ENABLE_ASSERTS=ON && cmake --build build/",
            "problemMatcher": []
        }
    ]
}
